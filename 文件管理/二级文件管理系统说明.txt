用户：//创建文件夹（登录名为文件夹名）
	登录
	创建（判断用户名是否已存在）：用户名，密码
	查询
	删除


文件：//对应每个用户
	新建（判读文件是否已存在）
	打开（判断是否已存在）
	关闭
	修改（判读文件是否已存在）
	删除（判读文件是否已存在）
	显示文件信息：文件名，文件长度，文件类型，创建时间（判读文件是否已存在）
	查看用户所有文件
写入fileMsg：文件名+起始位置+大小+属性+创建时间
usespace：已使用空间数


注：每个用户对应各自文件系统。用户内部文件不可重名，用户之间文件可以重名。